charm_name: iscsi-connector

smoke_bundles:
    - focal

gate_bundles:
    - bionic
    - focal

configure:
 - iscsi-connector-func-tests.setup.basic_target_setup
 - iscsi-connector-func-tests.setup.configure_iscsi_target

tests:
 - iscsi-connector-func-tests.tests.ISCSIConnectorTest

target_deploy_status:
  iscsi-connector:
    workload-status: blocked
    workload-status-message: "Missing"
